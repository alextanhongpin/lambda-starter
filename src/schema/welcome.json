{
    "id": "welcome.json",
    "$schema": "http://json-schema.org/draft-06/schema#",
    "title": "Welcome Email Template",
    "description": "Welcome email template",
    "type": "object",
    "properties": {
        "model": {
            "properties": {
                "keyword": { "type": "string", "minimum": 3 },
                "location": { "type": "string", "minimum": 3 }
            },
            "required": [
                "keyword", "location"
            ]
        }
    },
    "required": ["models"]
}
{
    "id": "greet.json",
    "$schema": "http://json-schema.org/draft-06/schema#",
    "title": "Greet",
    "description": "Greeting Service Request",
    "type": "object",
    "definitions": {
        "greet": {
            "properties": {
                "name": { "type": "string", "minLength": 3 }
            },
            "required": [
                "name"
            ]
        }
    },
    "allOf": [
        { "$ref": "#/definitions/greet" }
    ]
}